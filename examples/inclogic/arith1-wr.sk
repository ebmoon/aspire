void flip(int x, boolean h, ref int x_out) {
    x_out = x;
    if(h) { x_out = -x; }
}

void pre(int x, int a, int b, ref boolean ret) {
    ret = (a <= x && x <= b);
}

void is_pos(int x, ref boolean out) {
    out = x > 0;
}
