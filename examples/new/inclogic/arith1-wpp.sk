void flip(int x, int a, int b, boolean h, ref int x_out) {
    x_out = x;
    if(h) { x_out = -x; }
}

void post(int x_out, int a, int b, ref boolean ret) {
    ret = (a <= x_out && x_out <= b);
}

void is_pos(int x, ref boolean out) {
    out = x > 0;
}
